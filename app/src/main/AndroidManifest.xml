<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Foreground notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Location -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_splashscreen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_splashscreen"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgLAM"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ongoingActivity.OngoingBaseActivity"
            android:launchMode="singleTop"  />
        <activity
            android:name=".ui.ongoingActivity.OngoingGpsActivity"
            android:launchMode="singleTop"  />
        <activity
            android:name=".ui.ongoingActivity.OngoingPedometerActivity"
            android:launchMode="singleTop"  />
        <activity
            android:name=".ui.ongoingActivity.OngoingGpsPedometerActivity"
            android:launchMode="singleTop"  />

        <service
            android:name=".background.activityServices.BaseService"
            android:foregroundServiceType="location" />
        <service
            android:name=".background.activityServices.GpsService"
            android:foregroundServiceType="location" />
        <service
            android:name=".background.activityServices.PedometerService"
            android:foregroundServiceType="location" />
        <service
            android:name=".background.activityServices.GpsPedometerService"
            android:foregroundServiceType="location" />
        <service
            android:name=".background.activityServices.ActivityRecognitionService"
            android:foregroundServiceType="location" />

        <receiver
            android:name=".background.ActivityRecognitionBroadcastReceiver"
            android:exported="false"
            android:permission="com.google.android.gms.permission.ACTIVITY_RECOGNITION">
            <intent-filter>
                <action android:name="action.TRANSITIONS_DATA" />
            </intent-filter>
        </receiver>
    </application>

</manifest>